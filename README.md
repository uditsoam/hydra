1. What is Hydra?

Hydra is a password cracking tool used in penetration testing.

It tries multiple combinations of:

usernames
passwords

to find valid login credentials for a system.

It supports many services like:

SSH
FTP
HTTP login forms
Databases

Simple definition:
Hydra automatically tries many passwords very fast to break a login.
